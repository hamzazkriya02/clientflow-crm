import { requireUser } from "@/lib/auth-helpers";import { prisma } from "@/lib/prisma";import { ClientManager } from "@/components/dashboard/client-manager";
export default async function ClientsPage({searchParams}:{searchParams:Promise<{new?:string}>}){const u=await requireUser();const p=await searchParams;const clients=await prisma.client.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"}});return <div><p className="text-sm font-bold text-indigo-600">CRM</p><h1 className="mt-1 text-3xl font-black">Clients</h1><p className="mt-2 mb-6 text-sm text-slate-500">Manage contacts, relationships and connected work.</p><ClientManager initial={clients as any} openNew={p.new==="1"}/></div>}
