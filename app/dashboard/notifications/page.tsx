import { requireUser } from "@/lib/auth-helpers";import { prisma } from "@/lib/prisma";import { NotificationsList } from "@/components/dashboard/notifications-list";
export default async function Notifications(){const u=await requireUser();const items=await prisma.notification.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"},take:100});return <div><p className="text-sm font-bold text-indigo-600">Inbox</p><h1 className="mt-1 text-3xl font-black">Notifications</h1><p className="mb-6 mt-2 text-sm text-slate-500">Deadlines, invoice events and important CRM activity.</p><NotificationsList initial={items as any}/></div>}
