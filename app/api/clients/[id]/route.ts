import { NextRequest,NextResponse } from "next/server";import { auth } from "@/auth";import { prisma } from "@/lib/prisma";import { logActivity } from "@/lib/activity";
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:"Unauthorized"},{status:401});const{id}=await params;const exists=await prisma.client.findFirst({where:{id,userId:s.user.id}});if(!exists)return NextResponse.json({error:"Not found"},{status:404});const b=await req.json();const item=await prisma.client.update({where:{id},data:{name:b.name,company:b.company||null,email:b.email||null,phone:b.phone||null,country:b.country||null,address:b.address||null,website:b.website||null,status:b.status,notes:b.notes||null}});await logActivity(s.user.id,`Updated client ${item.name}`,"Client",id);return NextResponse.json({item})}
export async function DELETE(_:NextRequest,{params}:{params:Promise<{id:string}>}){const s=await auth();if(!s?.user?.id)return NextResponse.json({error:"Unauthorized"},{status:401});const{id}=await params;const exists=await prisma.client.findFirst({where:{id,userId:s.user.id}});if(!exists)return NextResponse.json({error:"Not found"},{status:404});await prisma.client.delete({where:{id}});await logActivity(s.user.id,`Deleted client ${exists.name}`,"Client",id);return NextResponse.json({ok:true})}
