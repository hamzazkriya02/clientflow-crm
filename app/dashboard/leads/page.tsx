import { requireUser } from "@/lib/auth-helpers";import { prisma } from "@/lib/prisma";import { LeadBoard } from "@/components/dashboard/lead-board";
export default async function LeadsPage(){const u=await requireUser();const leads=await prisma.lead.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"}});return <div><p className="text-sm font-bold text-indigo-600">Sales</p><h1 className="mt-1 text-3xl font-black">Lead Pipeline</h1><p className="mb-6 mt-2 text-sm text-slate-500">Drag opportunities between stages and convert wins into clients.</p><LeadBoard initial={leads as any}/></div>}
